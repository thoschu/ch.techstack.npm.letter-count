#!/usr/bin/env node

const Util = require('util'),
    R = require('ramda');

let result = R.concat(R.concat(R.nth(-1, R.prop('argv', process)), ' : '), R.toString(require('../index.js').letterCount(R.nth(-1, R.prop('argv', process)))));

Util.log(result);